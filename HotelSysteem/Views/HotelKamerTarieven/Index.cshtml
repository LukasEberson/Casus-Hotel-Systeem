@model IEnumerable<HotelSysteem.Models.HotelKamerTarief>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Kamer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GeldingVan)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GeldigTot)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tarief)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Kamer.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GeldingVan)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GeldigTot)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tarief)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
